[theme]
# 기본 액션 버튼은 녹색 계열로 유지
primaryColor="#2E7D32"
backgroundColor="#FFFFFF" # 깔끔한 흰색 배경
secondaryBackgroundColor="#F0F2F6" # 사이드바 및 위젯 배경 (밝은 회색)
textColor="#262730" # 가독성 높은 어두운 텍스트 색상
font="sans serif" # 깔끔한 산세리프 폰트

[client]
toolbarMode = "minimal"

[server]
port = 80
