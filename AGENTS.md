# AGENTS.md

이 저장소에서 작업할 때 항상 우선 참고하는 로컬 작업 규칙. 애매하면 "짧고 실행 가능하며
검증 가능한 규칙"으로 해석한다.

## 1. 최우선 규칙

1. 요구를 100% 이해할 때까지 질문하고, 이해한 뒤에 작업을 시작한다.
2. 사용자가 "질문"을 하면 바로 수정하지 말고 먼저 답한다.
3. 코드 주석은 한국어로 쓴다.
4. 답하는 중 수정할 것을 발견하면, 답변 뒤에 문제점과 수정 필요 여부를 함께 알린다.
5. 명시적으로 수정·구현을 요청받으면 계획 문서보다 실제 코드 수정과 검증을 우선한다.
6. 추측을 말하지 않는다. 소스 분석·DB 조회로 확인한 사실만 답한다.

## 2. 작업 전 확인

1. 새 모듈/도메인이거나 사용자가 문서를 언급한 경우에만 `README.md`·관련 문서를 읽는다.
2. `znotes/` 는 사용자 메모다 — 읽지도 참조하지도 않는다.
3. 리팩토링·구조 변경 시 `Dockerfile`·배포 스크립트·실행 진입점 영향을 확인한다.

## 3. 구현 원칙

1. 암묵적 fallback, silent default, 임의 보정 로직을 만들지 않는다.
2. 함수 기본값으로 동작을 암묵적으로 결정하지 않는다.
3. 필수 설정값이 없으면 즉시 명확한 에러를 낸다.
4. **표시 여부를 데이터 값 유무로 정하지 않는다.** "값이 있으면 컬럼을 보여준다" 식이면
   수집이 깨져 값이 비었을 때 화면이 조용히 사라져 사용자가 눈치채지 못한다. 설정·구조
   (국가·구분 등)로 판정하고, 빈 값은 빈 값 그대로 보여준다.
5. 함수는 짧게, 한 가지 역할만. 여러 책임이 섞이면 분리를 먼저 검토한다.
6. 실행 환경을 맞추려고 코드 수준을 낮추지 않는다(최신 타입 힌트를 구버전 문법으로 되돌리는 식).
   대신 프로젝트가 의도한 버전과 올바른 실행 환경을 찾는다.

### 3-1. 기존 코드 재사용 (신규 코드 최소화)

1. **언제**: 이미 있는 화면·기능과 같거나 비슷한 UI/로직을 만들 때.
2. **무엇을**: 새로 짜기 전에 `rg`/Grep 으로 기존 구현을 찾고, 있으면 그대로 재사용한다.
3. 재사용 대상이 특정 화면에 인라인으로 묶여 있으면 **공용 컴포넌트/훅/함수로 추출**해
   양쪽이 같은 코드를 쓰게 한다.
4. 같은 패턴을 다른 화면에 붙일 때는 **그 화면의 구현 방식을 표준으로 맞춘다.**
5. **발견 즉시 표준화**: 같은 역할인데 화면마다 제각각인 UI 요소를 발견하면 그 자리에서
   공용 컴포넌트로 통일한다. 겉모습(크기·모양)은 공용 컴포넌트 자체 스타일로 못 박고
   색상만 테마 변수를 따른다 — 전역 클래스에 겉모습을 의존시키지 않는다.
6. **판정·규칙도 공용 함수로**: UI 뿐 아니라 *조건·계산·분류* 가 두 곳 이상에서 필요하면
   함수 하나로 빼서 같이 쓴다. 화면마다 조건을 따로 두면 하나만 고쳐져 결과가 갈린다.
   예: 업종 노출 여부(`web/lib/pool-industry`), 순위 점수·업종 상한(`core/strategy/scoring`),
   셀렉트 선택지(`config.py`).
7. **예외**: 대응 구현이 전혀 없는 신규 기능일 때만 새로 짜고, 이유를 한 줄로 밝힌다.
   표준화 범위가 현재 작업과 무관하게 커지면 한 줄로 알리고 별도 진행 여부를 확인한다.

### 3-2. 죽은 코드·기능·데이터 즉시 제거

0. **가장 중요한 일은 코드를 지키는 게 아니라 줄이는 것이다.** 찌꺼기를 발견하면 묻지 않아도
   먼저 제안한다.
1. 기능을 바꾸거나 대체하면 **그 작업 안에서 옛 경로를 지운다.** "일단 두고 나중에"는 없다.
2. **제거 대상**: 안 쓰는 함수·분기·상수·설정 키, 대체된 옛 화면·라우트·API, 옛 필드/컬렉션,
   참조되지 않는 마이그레이션 백업 값.
3. **완료 조건**: 새 경로가 도는 것으로 끝이 아니라 **옛 경로가 코드·DB·문서에서 사라져야** 끝이다.
4. **DB도 코드와 같이 본다**: 코드에서 지운 필드는 DB 에서도 지운다. 순서는
   새 키 생성 → 이전 → 동작 확인 → 옛 키 삭제.
5. **판정**: 지우기 전 `rg`/Grep 으로 전 저장소를 확인한다. 동적 생성 키는 생성 코드까지 본다.
6. **두 종류**: ① 아무도 안 쓰는 것 → 삭제. ② 코드는 쓰는데 편집 화면이 없는 것 → 화면에
   노출하거나 의존 자체를 없앤다. 방치하지 않는다.
7. **중요도에 맞게**: 핵심 데이터(스냅샷·거래 이력·사용자가 넣은 설정)는 확인받고 백업 후 제거.
   그 외(파생·캐시·플래그·옛 잔재)는 안 쓰는 것 같으면 그냥 제거한다.
8. **판단은 추측이 아니라 확인으로**: "없으면 문제가 생긴다"고 말하기 전에 호출 흐름을 끝까지
   따라가 무엇이 깨지는지 데이터로 확인한다.
9. **예외**: 제거 범위가 현재 작업과 무관하게 커지면 한 줄로 알리고 별도 진행 여부를 확인한다.

## 4. 실행 및 검증

1. 시스템 `python`을 가정하지 않는다 — 프로젝트 가상환경의 Python 을 먼저 확인해 쓴다.
2. ① 다중 파일 변경 ② 타입/시그니처 변경 ③ 새 의존성 중 하나면 `.pre-commit-config.yaml`
   기반 검증을 돌린다. 한두 줄 수정이나 CSS/문구 수정은 생략한다.
3. 검증은 수정 범위와 직접 관련된 것부터 최소 단위로 실행한다.

## 5. 문서 동기화

1. 다음 변경이면 작업 끝에 `docs/` 를 갱신한다: 실행 진입점·명령 추가/제거, 운영 방식·아키텍처
   전환, 화면·모듈·배치·외부 API 추가/제거(목록에 한 줄), 환경변수 변경, 데이터 모델·스키마 변경,
   전략 규칙 변경이나 시도 후 폐기.

2. 문서는 3개뿐이다. 새 문서를 만들지 않는다:
   - 구조·모듈·DB 문서·로컬 실행·코드 규칙 → `docs/developer_guide.md`
   - 전략 규칙과 폐기한 시도(이유 포함)    → `docs/strategy_logic.md`
   - 배포·배치·인프라·도메인·환경변수     → `docs/server_infrastructure.md`

3. **문서는 유지보수용이다. 사용법은 적지 않는다.** 독자는 이 코드를 고치는 사람이지 화면을 쓰는 사람이 아니다.
   - **적는 것**: 무엇이 있고(화면·모듈·배치·DB 문서) 어디가 단일 소스인지, 서로 어떻게 연결되는지,
     코드만 봐서는 알 수 없는 결정·제약(왜 이렇게 했는지, 무엇을 시도해 버렸는지), 운영에 필요한
     경로·명령·환경변수.
   - **절대 적지 않는 것**: 화면 사용 순서·조작법, 버튼·셀렉트·토글이 뭘 하는지, 컬럼 목록, 설정값의
     선택지·범위·기본값, 계산식 세부, 단계별 튜토리얼, 끝난 이전 절차, 코드·상수·docstring 이 이미
     말하는 내용. 이런 것은 바뀔 때마다 문서가 같이 낡으므로 코드가 단일 소스다.
   - 설정값·UI 동작이 바뀌는 변경은 문서를 **갱신하지 않는다**. 문서에 이미 적혀 있다면 고치는 대신 지운다.
   - 분량 기준: 화면 하나·전략 하나는 몇 줄이면 충분하다. 표는 "무엇이 있는지" 목록에만 쓴다.
   - **예외**: 겪은 문제와 조치처럼 다시 막힐 수 있는 것은 한 줄 표로 남긴다.

## 6. 작업 방식

1. 기본은 순차 처리다.
2. Frontend·Backend 가 함께 있으면 **UI 를 먼저 만들어 확인받은 뒤** Backend 를 만든다.
3. Implementation Plan·Task·Walkthrough 같은 중간 산출물 문서는 만들지 않는다.
   사용자 요청이나 꼭 필요한 경우에만 짧게 쓴다.

## 7. Git 규칙

1. 명시적 요청이 없으면 `git commit`·`git push` 를 실행하지 않는다.
2. 코드 변경을 마치면 바로 쓸 수 있는 커밋 메시지를 함께 준다.
3. 커밋 요약은 한국어 1줄, 20단어 이내.

## 8. 해석 우선순위

1. 사용자의 현재 요청 → 2. 이 `AGENTS.md` → 3. `README.md` 와 관련 문서 → 4. 기존 코드 관례

## 9. 토큰 효율

1. 이미 읽은 파일은 다시 읽지 않는다. Edit/Write 가 성공했으면 재확인 Read 도 생략한다.
2. 탐색은 좁게 — 디렉터리 일괄 Read 대신 `rg`/Grep 으로 좁히고 필요한 부분만 부분 Read.
3. Agent 호출은 사용자가 요청했거나 광범위 탐색이 꼭 필요할 때만. 단순 찾기·수정은 직접 한다.
4. WebSearch/WebFetch 는 외부 자료를 명시 요청받았을 때만.
5. 전체 빌드·테스트는 요청받았거나 회귀 위험이 있을 때만. 단순 텍스트·CSS·상수 변경은 생략.
6. 변경 내용을 코드블록으로 통째로 복사하지 않는다 — 파일 경로와 핵심 요약만.
7. 도구 호출은 가능하면 한 메시지에서 병렬로 묶는다.
8. 모호하면 한 줄로 확인 질문한다 — 잘못 구현해 다시 하는 비용보다 싸다.

## 10. 화면과 백테스트는 같아야 한다

1. 목표 종목·비중과 진입·청산·리밸런싱 판정은 각 전략의 백테스트 엔진이 단일 소스다.
   모멘텀·신고가는 `utils/slot_backtest.py`, 포트폴리오는 `utils/portfolio_backtest.py`를 쓴다.
   화면·슬랙·합성은 저장된 전략 시작일·조건으로 계산한 결과를 읽기만 한다.
2. 실제 계좌 보유는 목표 종목·비중·수량 계산에 사용하지 않는다. 목표를 확정한 뒤
   실제 보유와의 차이를 매매 수량으로 계산할 때만 사용한다.
3. 합성은 전략별 배분·중복 종목 합산·계좌 금액 환산만 한다. 정수 주수·환율·체결가 등
   불가피한 차이는 구분하되, 보유 유지 보정이나 엔진에 없는 문턱·밴드를 추가하지 않는다.
4. 성과 비교 백테스트의 조회 기간은 실운용 시작일과 별개다. 같은 시작일·조건·가격 데이터에서는
   개별 운용 현황과 합성의 전략 내부 목표가 같아야 한다.
5. 사용자의 보유 종목이나 현금 비중을 근거로 합성 전략의 목표 비중·전략 액션을 만들거나 보정하지 않으며, 불가피한 체결·정수 수량 차이를 제외하고 백테스트 로직과 99.9% 동일한 운용을 목표로 한다.

## 11. 이 문서의 의도

1. 긴 설명보다 바로 쓸 수 있는 판단 기준을 남긴다.
2. 새 규칙은 "언제 · 무엇을 · 예외"가 바로 드러나게 쓴다.
