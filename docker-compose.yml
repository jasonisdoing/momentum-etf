services:
  app:
    build: .
    command: python run.py
    env_file:
      - .env
    environment:
      - TZ=Asia/Seoul
    ports:
      - "8501:8501"
    volumes:
      - ./logs:/app/logs
      - ./.streamlit:/app/.streamlit

  scheduler:
    build: .
    command: python aps.py
    env_file:
      - .env
    environment:
      - TZ=Asia/Seoul
    volumes:
      - ./logs:/app/logs
