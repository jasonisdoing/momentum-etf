version: '3.8'

services:
  scheduler:
    container_name: etf
    build: .
    env_file:
      - .env
    environment:
      - TZ=Asia/Seoul
    command: python scheduler.py
    volumes:
      - ./logs:/app/logs
